import { Store } from "./store"
class {
    mobxObservable(){}
    onCreate() {
        this.renderCount = 0
    }
    onRender() {
        this.renderCount++
    }
    onMount () {
        window.app = this;
    }
}
div.count key="count" -- ${Store.count}
view-one
view-two